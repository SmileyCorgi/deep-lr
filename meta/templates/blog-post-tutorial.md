---
title: {{主题}} — {{副题，用 em-dash 不用冒号}}
date: {{date}}
slug: {{kebab-case-slug}}
category: paper-research
status: draft
dek: {{≤140 字一句话：这篇教什么、覆盖哪几块}}
tags: [{{tag1}}, {{tag2}}]
focus_label: 全景速览
---

<!--
教学长文（tutorial）文体模板 — 固化自 llm-training-tech-map（2026-07）。
Lilian Weng 式结构：系统性、编号长文、图解优先、每项技术带原始论文。
硬性约定（渲染器契约 + 实测踩坑）：
  1. 无 LaTeX 渲染 —— 公式一律行内代码：`L = -log σ(r_w − r_l)`。
  2. canvas 块内禁止 [[wikilink]]；引用用 source: 或 [text](url)。
  3. matrix 的 rows 用多字母标签（`DP | TP | PP`）——单字母 A/T/M/S 会被
     渲染器自动别名成 Agent/Tool/Memory/Skill。
  4. 每个关键数字带论文出处位置（§x.y / Table N）。
  5. [[entity]] 引用只给会反复出现的锚点论文（建实体页）；长尾文献用
     文末分组的普通链接清单。
  6. 中英混排时检查形近非 ASCII 字符（如西里尔字母）混入。
-->

## The gist

- {{3–6 条：每条一句话讲清一条主线，读者只读这里也能带走地图}}
- {{主线之间的关系（"X 是 Y 的前提"这种结构性论断放这里）}}

## 1. 全景：{{把整个主题装进一张图}}

{{1–3 句：这张图的读法与本文行进路线。}}

```canvas
type: flow
caption: {{全景 pipeline + 关键回环}}
source: {{arXiv id / §位置}}
A -> B -> C -> D
loop: D -> B | {{回环语义}}
note: {{一条最反直觉的注记}}
```

## 2. {{第一个技术板块}}：{{切什么，代价在哪}}

{{1–3 句引入。}}

```canvas
type: matrix
caption: {{速查表：维度 × 判断标准}}
rows: {{XX | YY | ZZ —— 多字母！}}
cols: {{是什么 | 代价在哪 | 何时必需}}
1,1 :: **{{名称}}** — {{机制一句话}}。`{{代表实现}}`
```

{{正文段落 + 常见误解拆解：}}
- **"{{最常见的误解}}"**——{{为什么错，正确的机制是什么}}。

## N. {{演化型板块用 timeline}}

```canvas
type: timeline
caption: {{谱系一句话 + 最容易讲错的一点}}
source: {{ids}}
{{年份}} | {{里程碑}} | {{一句话机制 + 关键数字}} ({{PILL}})
```

## N+1. {{枚举型板块用 grid}}

```canvas
type: grid
caption: {{这组卡片共同回答什么问题}}
cols: 3
{{名称}} :: {{机制一句 + 关键数字 + 坑}}。{{arXiv id}}
```

## 倒数第二节. 收束：一张学习地图

自查清单式收束——如果你只记 N 句话：

1. {{每条对应一个板块的一句话结论}}

想继续深挖：{{按板块给出"读哪几篇原文、什么顺序"}}。

**{{大师/经典博客}}延伸阅读**（与本文各节互补）：

| 文章 | 对应本文 | 关系 |
|---|---|---|
| [{{标题}}]({{url}})（{{年月}}） | §N | {{互补关系一句话}} |

## 最后一节. 完整文献清单

正文 `[[引用]]` 的核心文献见文末自动生成的 References。其余原始论文按主题分组：

**{{主题 A}}**
- {{标题}} — {{作者}} {{年}}, [arXiv:{{id}}](https://arxiv.org/abs/{{id}})
