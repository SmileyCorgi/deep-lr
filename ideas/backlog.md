# Ideas backlog

Prioritized tracker of forward-looking research questions. Append with date
and trigger condition; prune on promotion to `experiments/`.

Entry format: a heading `[YYYY-MM-DD] one-line idea` (H2, date in brackets),
followed by `- why: …` and `- trigger: …` bullets (what would make this worth
starting).

_(empty)_
